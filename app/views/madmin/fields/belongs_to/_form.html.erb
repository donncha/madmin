<%= form.select field.to_param, field.options_for_select(record), { prompt: true }, { data: { controller: "select", select_url_value: field.index_path, select_min_length_value: field.options.min_length } } %>
